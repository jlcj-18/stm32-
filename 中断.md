以 **STM32F103 的 PA0 上升沿中断**为例，把整个过程拆开。核心是三层硬件协作：
```
GPIO：读取引脚电平
EXTI：检测电平变化并产生中断请求
NVIC：决定CPU是否立即响应
```

## 1.软件的配置：
### GPIO配置为输入
PA0负责接收外部电压：
```
外部电压 → PA0 → GPIO输入缓冲器
```

### AFIO选择EXTI信号来源
STM32F103中，相同编号的GPIO共用一条EXTI线路：
```
PA0、PB0、PC0…… → EXTI0
PA1、PB1、PC1…… → EXTI1
```
但是EXTI0一次只能选择其中一个端口。因此需要通过AFIO告诉芯片：
```
EXTI0目前连接PA0
```
可以把AFIO理解成一个选择开关：
```
PA0 ─┐
PB0 ─┼── AFIO选择 ── EXTI0
PC0 ─┘
```

### EXTI选择触发边沿
EXTI可以检测：
- 上升沿：0 → 1
- 下降沿：1 → 0
- 双边沿：两种变化都触发
相关寄存器：
- RTSR：上升沿触发选择
- FTSR：下降沿触发选择
- IMR：是否允许该线路产生中断请求
- PR：挂起标志

### NVIC允许对应中断
EXTI只是提出请求，NVIC还需要允许这个中断：
```
EXTI请求 → NVIC使能位 → 优先级判断 → CPU
```
还要配置：
- 是否使能这个中断通道
- 抢占优先级
- 响应优先级
配置完成后，CPU继续正常运行主程序，等待外部事件。


## 2.实际执行流程
### 引脚电平发生变化
假设PA0按键为低电平，外部设备将其改为高电平，这就是一个上升沿。GPIO输入缓冲器把引脚电压转换成内部数字信号：
```
接近0V    → 逻辑0
接近3.3V  → 逻辑1
```
### EXTI检测边沿
EXTI内部有边沿检测电路。它会比较信号前后的状态：
```
前一次状态：0
当前状态：1
检测结果：出现上升沿
```
如果配置了上升沿触发，EXTI就认为触发条件成立。如果只配置了下降沿，这次变化不会触发中断。
可以把边沿检测简单理解为：
```
上一次采样值 != 当前采样值
```
然后再判断变化方向。
### EXTI挂起标志位置1
EXTI确认触发条件成立后，会将对应的挂起标志置1。
对于EXTI0：
```
EXTI_PR.PR0 = 1
```
这个标志表示：EXTI0线路曾经发生了一个需要处理的事件。它相当于EXTI留下一张“待处理通知单”。
即使引脚随后恢复原状态，这个标志通常仍会保持为1，直到软件主动清除。
注意STM32F1的EXTI挂起标志采用“写1清除”：
```
EXTI->PR = EXTI_Line0;
```
或者使用库函数：
```
EXTI_ClearITPendingBit(EXTI_Line0);
```
看起来是写入1，实际效果却是清零，这是硬件寄存器定义决定的。
### EXTI向NVIC提出请求
如果EXTI0没有被IMR屏蔽，EXTI会向NVIC发送中断请求：
```
EXTI0挂起标志 = 1
        +
EXTI0中断未屏蔽
        ↓
向NVIC请求中断

IMR屏蔽的作用：
例如串口正在接收数据，但你现在要执行一段不能被打断的重要代码
关闭串口接收中断;
IMR 屏蔽 执行重要代码; 
清除中断标志; 
打开串口接收中断; 
取消屏蔽。
```
如果IMR屏蔽了EXTI0：
```
EXTI仍然可能记录挂起标志
但不会请求CPU执行中断函数
```
因此，“记录事件”和“打断CPU”是两个不同动作。

### NVIC判断能否响应
NVIC是嵌套向量中断控制器。它收到请求后，不会无条件打断CPU，而是检查几个条件。
#### 1. 中断通道是否使能
例如EXTI0对应EXTI0_IRQn。如果NVIC没有使能这个通道，请求不会进入CPU。
#### 2.CPU是否全局屏蔽了中断
程序可以通过内核寄存器临时屏蔽普通中断，例如：
```
__disable_irq();
```
这会设置`PRIMASK`。此时NVIC可以记录请求，但CPU暂时不会响应。
重新开启：
```
__enable_irq();
```
如果请求仍然挂起，CPU随后可以响应。
#### 3.中断优先级是否足够高
如果CPU正在执行主程序，没有其他中断，一般可以响应。如果CPU已经在执行另一个中断，NVIC会比较抢占优先级：
```
新中断优先级更高 → 打断当前中断，形成嵌套
新中断优先级不高 → 等当前中断结束
```
在STM32中，优先级数值越小，优先级越高：
```
优先级0 > 优先级1 > 优先级2
```
#### 4. 当前指令是否可以结束
Cortex-M3通常会先完成当前正在执行的指令，再进入中断，不会随意把一条普通指令执行到一半。

### CPU自动保存现场
NVIC决定响应后，CPU需要暂时离开主程序。假设主程序执行到这里：
```
a = a + 1;
b = b + 1;   // 执行到附近时发生中断
c = c + 1;
```
CPU必须记住：
- 中断前程序执行到了哪里
- 部分寄存器原来是什么值
- 原来的运行状态
Cortex-M3会自动把以下内容压入栈：
```
R0
R1
R2
R3
R12
LR
PC
xPSR
```
其中最关键的是：
- `PC`：中断发生前，程序执行到的位置
- `LR`：函数返回相关地址
- `xPSR`：CPU运行状态
- `R0～R3、R12`：常用工作寄存器
这个过程叫做**现场保护**。
不是程序员手动一项项保存，Cortex-M3硬件会自动完成基本保存。编译器还可能在中断函数开头保存其他会被使用的寄存器。

### CPU查询中断向量表
中断向量表本质上是一张地址表：
```
异常或中断编号          对应处理函数地址
复位异常                Reset_Handler
SysTick                  SysTick_Handler
EXTI0                    EXTI0_IRQHandler
EXTI1                    EXTI1_IRQHandler
TIM2                     TIM2_IRQHandler
USART1                   USART1_IRQHandler
```
CPU已经从NVIC知道本次响应的是`EXTI0_IRQn`，所以不需要逐项搜索。它会根据中断编号计算向量表中的位置：
```
向量表基地址 + 对应编号偏移
```
然后读取该位置保存的函数地址。例如：
```
EXTI0_IRQn
→ 向量表中EXTI0的位置
→ 读取EXI0_IRQHandler的地址
→ 将该地址装入PC
```
注：这里的“查找”是硬件根据编号直接索引，并不是运行一个循环慢慢查。

### 执行中断处理函数
CPU将中断函数地址装入PC以后，开始执行：
```
void EXTI0_IRQHandler(void)
{
    if (EXTI_GetITStatus(EXTI_Line0) == SET)
    {
        // 处理按键或外部事件

        EXTI_ClearITPendingBit(EXTI_Line0);
    }
}
```
#### 为什么还要检查挂起标志？
因为部分中断向量由多个中断源共用。例：
```
EXTI5、EXTI6、EXTI7、EXTI8、EXTI9
            ↓
     EXTI9_5_IRQHandler
```
进入这个函数以后，需要检查究竟是哪一条EXTI线路触发。EXTI0拥有单独的中断函数，但检查标志仍是一种稳妥的写法。
#### 为什么必须清除挂起标志？
如果中断函数不清除EXTI挂起标志：
```
退出中断
→ EXTI请求仍然存在
→ NVIC再次通知CPU
→ CPU再次进入中断
```
程序可能不断执行中断函数，主循环几乎无法继续
通常应该先处理必要数据，然后及时清除标志。对于按键，还需要考虑机械抖动，否则一次按下可能产生多个边沿。

### 中断函数结束并恢复主程序
中断函数执行完毕后，编译器会生成特殊的异常返回指令。
Cortex-M3自动从栈中恢复之前保存的：
```
R0～R3、R12、LR、PC、xPSR
```
恢复PC后，CPU回到中断前的位置：
```
a = a + 1;
b = b + 1;   // 中断前执行到这里
c = c + 1;   // 恢复后继续
```
主程序通常感觉不到自己曾被暂停，只是中间插入了一次中断处理。






