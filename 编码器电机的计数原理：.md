编码器测速的核心其实就是一句话：在固定的时间内，数编码器产生了多少个脉冲，再换算成电机转了多少圈，最后得到转速。
## 1.编码器电机为什么能测速？
编码器电机后面一般带一个编码器，电机转动时，编码器会输出方波信号。常见的是 **AB 相正交编码器**：
	
```
A相：__--__--__--
B相: _--__--__--_
```
A 相和 B 相不是同时变化的，而是有一个相位差。这个相位差有两个作用：
1.通过脉冲数量判断了多少。
2.通过A B相先后关系判断转动方向。
## 2.脉冲数和转速的关系：
假设编码器一圈会产生 `300` 个脉冲。如果在 `0.02s` 内数到了 `60` 个脉冲，那么：

```
0.02 秒转了 60 / 300 = 0.2 圈
```
1 秒转多少圈：

```
0.2 / 0.02 = 10 圈/秒
```
换成 RPM，也就是每分钟转多少圈：

```
10 * 60 = 600 RPM
```
所以公式是：

```
RPM = 脉冲数 / 每圈脉冲数 / 采样时间 * 60
```
## 3.我的代码示例：
首先就是这三个关键变量
```
static int32_t Left_temp = 0;    //在一个采样周期内临时累加编码器脉冲
volatile int32_t Left_Num = 0;   //本次采样周期内的脉冲数
volatile int32_t Left_Count = 0; //从程序开始到现在的总累计脉冲数
```
我在编码器中断中做的是：

```
Left_temp++;

或者

Left_temp--;
```
也就是每检测到一个编码器边沿，就根据方向增加或减少计数。然后定时器每隔一段时间调用：

```
Encoder_Update();
```
里面做的是：
这一步很重要
```
Left_Num = Left_temp;
Left_Count += Left_Num;
Left_temp = 0;
```
<u>把这 20ms 内数到的脉冲保存到 Left_Num ，然后把临时计数 Left_temp 清零 ，准备下一次 20ms 重新计数。</u>

## 4.A B相方向的判断：
以左编码器 A 相触发为例：

```
if((gpio_status_L1 & ENCODER_Left_A_PIN) == ENCODER_Left_A_PIN)
{
    if(!DL_GPIO_readPins(ENCODER_Left_B_PORT, ENCODER_Left_B_PIN))
    {
        Left_temp++;
    }
    else 
    {
        Left_temp--;
    }
}
```
意思是：当 A 相发生变化时，读取 B 相当前电平，如果 B 相是低电平，就认为是一个方向，就：

```
Left_temp++;
```
如果 B 相是高电平，就认为是另一个方向：

```
Left_temp--;
```
所以编码器不是单纯地数脉冲，它还能根据 A、B 相的关系判断方向。
## 编码器1倍频，2倍频，4倍频计数
由于天猛星只有一个编码器外设（4倍频），没法用，就得自己写。于是就写了一个二倍频的编码器测速（精度较高，且抗干扰也较强）。所以这里就写一下倍频。

假设编码器一圈有 300 个周期。
### 一倍频
只检测 A 相的一个边沿，比如只检测上升沿：
一圈计数：300次。
### 二倍频
检测 A 相和 B 相各一个边沿：
A相上升沿计数，B相上升沿计数：一圈计数：300 * 2 = 600 次
### 四倍频
检测 A、B 两相的上升沿和下降沿：
300 * 4 = 1200 次
## 速度的换算：
一般都是带减速箱的，所以如果你要算的是**轮子转速**，要把减速比也算进去：
```
轮子 RPM = 编码器脉冲数 / (编码器PPR * 倍频数 * 减速比) / 采样时间 * 60
```
如果算的是**电机轴转速**，就不用乘减速比。

## 总结：
编码器测速本质上就是：

> **固定时间内数脉冲，用脉冲数除以每圈脉冲数，就知道这段时间转了多少圈，再除以采样时间得到转速。**